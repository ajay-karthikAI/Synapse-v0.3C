# Changelog — diabetes-previsit

All content changes require a version bump. A content change revokes any
existing pack approval; see `docs/source-governance.md`.

## 0.1.0 — 2026-08-12

- Pack initialised in `unapproved_example` state.
- Added 4 synthetic placeholder sources (3 `discovered`, 1 `superseded`).
- **No reviews recorded. No sources approved.** This pack demonstrates the governance
  structure only; advancing any source past `discovered` requires a real clinician
  review filed with `source_pack review`.
